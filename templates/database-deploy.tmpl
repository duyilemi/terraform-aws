sudo apt update
sudo apt install git mysql-client -y
git clone -b vp-rem https://github.com/duyilemi/java-webapp.git
mysql -h ${rds-endpoint} -u ${dbuser} --password=${dbpass} accounts --ssl-mode=DISABLED < /home/ubuntu/java-webapp/src/main/resources/db_backup.sql
